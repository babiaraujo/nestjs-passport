{
  "compilerOptions": {
    "module": "commonjs",                     // Módulo usado pelo Node.js
    "declaration": true,                      // Gera arquivos de declaração .d.ts
    "removeComments": true,                   // Remove comentários no código de saída
    "emitDecoratorMetadata": true,            // Emite metadados de tipo para decorators
    "experimentalDecorators": true,           // Habilita o uso de decorators
    "allowSyntheticDefaultImports": true,     // Permite importações padrão sintéticas
    "esModuleInterop": true,                  // Habilita interoperabilidade com módulos ES
    "target": "ES2021",                       // Versão de JavaScript de saída (pode ajustar para "ES6")
    "sourceMap": true,                        // Gera mapas de fonte para depuração
    "outDir": "./dist",                       // Diretório de saída dos arquivos compilados
    "baseUrl": "./",                          // Base URL para resolver caminhos de módulos
    "incremental": true,                      // Habilita a compilação incremental
    "skipLibCheck": true,                     // Ignora verificação de tipo de arquivos de declaração
    "strict": true,                           // Habilita todas as verificações de tipo rigorosas
    "strictNullChecks": false,                // Verificação rigorosa de nulos (pode ser ajustado)
    "noImplicitAny": false,                   // Não permite variáveis implícitas de tipo "any"
    "strictBindCallApply": false,             // Verificação rigorosa de chamadas e aplicações de bind
    "forceConsistentCasingInFileNames": true, // Verifica se o uso de letras maiúsculas/minúsculas nos nomes de arquivos é consistente
    "noFallthroughCasesInSwitch": true,       // Não permite casos de fallthrough em switch
    "moduleResolution": "node"                // Resolve os módulos como o Node.js
  },
  "include": ["src/**/*"],                    // Inclui todos os arquivos TypeScript na pasta src
  "exclude": ["node_modules"]                 // Exclui a pasta node_modules
}
